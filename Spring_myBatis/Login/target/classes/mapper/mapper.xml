<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="test">
 	<insert id="insert1" parameterType="kr.hs.study.model.dto.LoginBean">
	insert into login1(id,pw) values(#{id},#{pw}) 	
 	</insert>
 	<delete id="delete1">
 	delete from login1 where id=#{id}
 	</delete>
 	<select id="list1" resultType="kr.hs.study.model.dto.LoginBean">
 		select * from login1 order by id desc
 	</select>
</mapper>  






     